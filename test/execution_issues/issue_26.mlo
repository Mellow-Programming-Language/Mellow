// ISSUE: Issue 26
// EXPECTS: "Correct Length! First Things ' Stuff New String"

import std.io;

func main() {
    str := "First " ~ "Things \' Stuff " ~ "New " ~ "String";
    if (str.length == 31) {
        write("Correct Length! ");
    }
    writeln(str);
}
